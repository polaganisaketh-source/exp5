pipeline {
    agent any
    stages {
          stage('compile') {
                steps {
                     sh 'javac Names.java'
                     }
                   }
                   stage('Run') {
                      steps{
                          sh 'echo "sandeep" | java Names'
                           }
                          }
                         }
                        }
